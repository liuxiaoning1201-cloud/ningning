# 最簡 AI 角色聊天

3 個檔案，零複雜度。

## 啟動

```bash
cd simple_chat
pip install -r requirements.txt
python app.py
```

瀏覽器開啟 http://localhost:5000

## 檔案

- `app.py` - 後端（含 Flask + DeepSeek 調用）
- `index.html` - 前端
- `.env` - API 金鑰（已加入 .gitignore）
