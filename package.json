{
  "name": "ningning",
  "version": "1.0.0",
  "private": true,
  "description": "字遊空間靜態站點",
  "scripts": {
    "pages:build": "node scripts/prepare-pages.mjs",
    "pages:deploy": "npm run pages:build && wrangler pages deploy .pages-deploy --project-name=ziyoukongjian",
    "pages:deploy-full": "INCLUDE_POETPAL=1 npm run pages:build && wrangler pages deploy .pages-deploy --project-name=ziyoukongjian",
    "pages:create": "wrangler pages project create ziyoukongjian --production-branch=main"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
