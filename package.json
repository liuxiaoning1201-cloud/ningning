{
  "name": "ningning",
  "version": "1.0.0",
  "private": true,
  "description": "字遊空間靜態站點",
  "scripts": {
    "build": "cd apps/中文填字接龍 && npm install && cd ../班級守護隊 && npm install --legacy-peer-deps && cd ../句子排序小火車 && npm install && cd ../.. && node scripts/build.mjs",
    "pages:deploy": "npm run build && npx wrangler pages deploy dist --project-name zykongjian"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "5.4",
    "wrangler": "^4"
  }
}
